apiVersion: apps/v1
kind: Deployment
metadata:
  name: emulationstation
  namespace: gaming
spec:
  replicas: 1
  selector:
    matchLabels:
      app: emulationstation
  template:
    metadata:
      labels:
        app: emulationstation
    spec:
      nodeSelector:
        kubernetes.io/hostname: belchior-pi
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        supplementalGroups: [44, 46, 992, 996]  # video, plugdev, render, input
      containers:
        - name: emulationstation
          image: ghcr.io/belchiorg/emulationstation:latest
          imagePullPolicy: Always
          env:
            - name: WAYLAND_DISPLAY
              value: wayland-0
            - name: XDG_RUNTIME_DIR
              value: /tmp/wayland-runtime
            - name: SDL_VIDEODRIVER
              value: wayland
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "3500m"
              memory: "2Gi"
          volumeMounts:
            - name: wayland-socket
              mountPath: /tmp/wayland-runtime/wayland-0
            - name: config
              mountPath: /config
            - name: roms
              mountPath: /roms
            - name: dri
              mountPath: /dev/dri
            - name: input
              mountPath: /dev/input
      volumes:
        - name: wayland-socket
          hostPath:
            path: /run/user/1000/wayland-0
            type: Socket
        - name: config
          hostPath:
            path: /mnt/data/es-config
            type: Directory
        - name: roms
          hostPath:
            path: /mnt/data/roms
            type: Directory
        - name: dri
          hostPath:
            path: /dev/dri
            type: Directory
        - name: input
          hostPath:
            path: /dev/input
            type: Directory
